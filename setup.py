from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='This project will use NYC LiDAR data to predict the height of the mixing layer height as well as the residual layer.',
    author='Jenifer Vivar',
    license='MIT',
)
